// Página preservada para expansão futura com formulário integrado

import type { Metadata } from 'next';

export const metadata: Metadata = {
  robots: 'noindex',
};

export default function ContatoPage() {
  return (
    <main style={{ padding: '3rem 1rem', maxWidth: 900, margin: '0 auto' }}>
      <h1>Contato</h1>
      <p>Em breve, formulário completo para solicitação de orçamentos.</p>
    </main>
  );
}
