// Página preservada para expansão futura com conteúdo detalhado dos serviços

import type { Metadata } from 'next';

export const metadata: Metadata = {
  robots: 'noindex',
};

export default function ServicosPage() {
  return (
    <main style={{ padding: '3rem 1rem', maxWidth: 900, margin: '0 auto' }}>
      <h1>Serviços</h1>
      <p>Em breve, detalhamento completo dos nossos serviços editoriais.</p>
    </main>
  );
}
